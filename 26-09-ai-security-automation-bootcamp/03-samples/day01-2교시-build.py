# -*- coding: utf-8 -*-
# Day 1 · 2교시 파이썬 기초 교안 v5 — 기초부터 쉽게, 2레이어, 시간표기('상세교안' 단어 미사용)
import json, os
cells=[]
def md(t): cells.append({"cell_type":"markdown","metadata":{},"source":t})
def code(t): cells.append({"cell_type":"code","metadata":{},"execution_count":None,"outputs":[],"source":t})

# ═══ 표지 ═══
md("""# Day 1 · 2교시 — 파이썬 기초 (강의1 후반)

## 변수·자료형 · 리스트·딕셔너리 · 연산자·형변환

> **만든 날짜:** 2026-07-13 · **버전:** v5 · **범위:** 강의1 55–120분

처음 코딩하는 분도 따라올 수 있게 **아주 기초부터** 설명합니다. 구성은 2단계입니다.

1. **본 수업** — 배정된 시간 안에 다루는 핵심.
2. **[심화·자습]** — 더 많은 예제·문제. 시간이 남거나 자습·리추얼 때 풀면 됩니다.""")

# ═══ 목차 ═══
md("""## 목차

1. **변수와 자료형** · 55–80분
2. **리스트와 딕셔너리** · 80–105분
3. **연산자와 형변환** · 105–120분

마무리 — 오늘 요약, 종합 퀴즈(객관식)""")

# ═══ 학습목표 ═══
md("""## 학습목표

이 시간이 끝나면 다음을 할 수 있습니다.

1. 변수가 무엇인지 설명하고, 값을 담아 꺼내 쓰기
2. 자료형 4가지(정수·실수·문자열·참거짓)를 구분하고 `type()`으로 확인하기
3. 관습에 맞는 변수 이름 짓기
4. f-string으로 값을 문장에 끼워 출력하기
5. 리스트·딕셔너리로 여러 값 다루기, 형변환의 함정 이해하기""")

# ═══ 오늘 만드는 것 ═══
md("""## 오늘 만드는 것

보안 관제 대상 고객사 `hanbit` 의 로그 한 줄을 파이썬으로 표현하고,
`[hanbit] failed_login=7, alert=True` 같은 요약 문장을 출력합니다.
(오후 실습 `agent_core/day01_basic.py` 로 이어집니다.)""")

# ═══ 용어 풀이 ═══
md("""## 용어 풀이

| 용어 | 쉽게 말하면 |
|------|-------------|
| **값(value)** | 컴퓨터가 다루는 데이터 하나. 숫자 `7`, 글자 `"kim"` 등 |
| **변수(variable)** | 값을 담는 그릇 = 값에 붙이는 이름표 |
| **자료형(type)** | 값의 종류(정수·실수·문자·참거짓) |
| **f-string** | 문장에 값을 끼워 넣어 출력하는 방법 |
| **리스트 / 딕셔너리** | 여러 값을 순서대로 `[...]` / 이름표로 `{키:값}` |""")

# ═══ 사용법 ═══
md("""## 이 노트북 쓰는 법

- 회색 코드 칸을 클릭하고 **`Shift + Enter`** 로 **한 칸씩** 실행합니다.
- 칸의 역할은 5가지 표시로 구분합니다.

| 표시 | 뜻 |
|------|-----|
| 📖 | 개념 — 읽는 칸 |
| 💻 | 예제 — 그대로 실행해 결과 확인 |
| ✍️ | 문제 — 직접 풀어 보는 칸 |
| ✅ | 결과 — "이렇게 나오면 성공" |
| ❓ | 면접 질문 — 답은 본문 안에 있음(스스로 찾기) |

> 각 섹션은 **본 수업** 다음에 **`[심화·자습]`** 이 옵니다. 본 수업만 따라가도 충분합니다.""")

# ══════════════════════════════════════════════════
# 1. 변수와 자료형 · 55–80분
# ══════════════════════════════════════════════════
md("""---
## 1. 변수와 자료형 · 55–80분""")

# 1-0 왜 변수부터?
md("""### 📖 왜 '변수'부터 배울까?

1. **프로그램은 결국 값을 다루는 일**입니다. 로그인 실패 횟수, 사용자 이름, 경보 여부 — 전부 '값'입니다.
2. 그 값을 어딘가 **담아 두어야** 나중에 꺼내 쓰고, 비교하고, 계산합니다. 그 담는 그릇이 **변수**입니다.
3. 오늘 배우는 나머지(자료형·리스트·딕셔너리)도 결국 **"변수에 무엇을, 어떻게 담느냐"**의 이야기입니다.
4. **이 과정과의 연결**: 앞으로 만들 보안 자동화 봇은 "로그를 **변수에 담고** → 조건으로 걸러내고 → 알림 보내기"의 반복입니다.

> 🍳 **비유:** 변수는 재료를 담는 **그릇**입니다. 그릇이 있어야 요리를 시작할 수 있습니다.""")

# 1-1 변수란
md("""### 📖 변수 = 값에 붙이는 '이름표'

- 변수는 `이름 = 값` 으로 만듭니다. 예: `x = 7` → "값 7에 `x`라는 이름표를 붙임".
- 한 번 만들면, 그 **이름으로 값을 꺼내** 씁니다.
- 값은 나중에 바꿀 수 있습니다(재할당). 같은 이름에 새 값을 넣으면 됩니다.

> **주의 — `=` 는 "같다"가 아닙니다.**
> - `=` : "값을 **넣어라**"(대입)
> - `==` : "**같은지** 물어봐라"(비교) — 뒤 3번에서 다룸""")

code("""# 💻 예제 — 가장 간단한 변수 하나
name = "kim"        # "kim" 이라는 값에 name 이라는 이름표를 붙임
print(name)         # 이름표로 값을 꺼내 출력""")
md("""**✅ 성공 출력**
```
kim
```""")

code("""# 💻 예제 — 값을 바꾸기(재할당)
count = 3
print(count)        # 3
count = 10          # 같은 이름에 새 값을 넣음
print(count)        # 10""")
md("""**✅ 성공 출력**
```
3
10
```""")

code("""# ✍️ 문제 (출력 예측) — 아래가 무엇을 출력할지 먼저 예측한 뒤 실행하세요.
city = "seoul"
city = "busan"
print(city)         # 예측: ?""")

# 1-2 자료형 기초
md("""### 📖 값에는 '종류'가 있습니다 = 자료형

사람도 "숫자 7"과 "글자 ㄱ"을 다르게 다루듯, 컴퓨터도 값의 **종류**를 구분합니다. 기본 4가지입니다.

1. **문자열 `str`** — 글자(문자)들의 나열. **반드시 따옴표로 감쌉니다.** 예: `"kim01"`, `"login_failed"`
   - 따옴표가 "이건 글자야"라고 컴퓨터에 알려주는 표시입니다. 작은따옴표 `'...'`, 큰따옴표 `"..."` 둘 다 됩니다.
2. **정수 `int`** — 소수점 없는 숫자. 따옴표 없이 씁니다. 예: `7`, `0`, `-3`
3. **실수 `float`** — 소수점 있는 숫자. 예: `73.5`, `0.23`
4. **참거짓 `bool`** — `True`(참) 또는 `False`(거짓) 둘 중 하나. 조건 판단에 씁니다.

- 값의 종류는 `type(값)` 으로 확인합니다.

> **왜 종류가 중요할까?** 같은 7이라도 **글자 `"7"`** 과 **숫자 `7`** 은 컴퓨터에게 완전히 다릅니다.
> (이 차이는 3번 '형변환'에서 실감하게 됩니다.)""")

code('''# 💻 예제 — 문자열(str): 글자는 따옴표로 감싼다
user = "kim01"
event = "login_failed"
print(user)
print(event)''')
md("""**✅ 성공 출력**
```
kim01
login_failed
```""")

code("""# 💻 예제 — 숫자: 정수(int)와 실수(float)
failed_login_count = 7      # 정수
cpu_usage = 73.5            # 실수(소수점)
print(failed_login_count)
print(cpu_usage)""")
md("""**✅ 성공 출력**
```
7
73.5
```""")

code("""# 💻 예제 — 참거짓(bool): True 또는 False
is_alert = True
is_blocked = False
print(is_alert)
print(is_blocked)""")
md("""**✅ 성공 출력**
```
True
False
```""")

code("""# 💻 예제 — 값의 '종류'를 type() 으로 확인
print(type("kim01"))   # str
print(type(7))         # int
print(type(73.5))      # float
print(type(True))      # bool""")
md("""**✅ 성공 출력**
```
<class 'str'>
<class 'int'>
<class 'float'>
<class 'bool'>
```
`<class 'str'>` 은 "이 값은 str(문자열) 종류"라는 뜻입니다.""")

code('''# ✍️ 문제 (빈칸 채우기) — 빈칸 ____ 을 알맞게 채우세요.  기대 출력 아래 참고.
port = ____        # 정수 8080 을 넣으세요
service = ____     # 문자열 "auth-server" 를 넣으세요
print(port)        # 기대: 8080
print(service)     # 기대: auth-server''')

md("""### ❓ 확인질문

`is_alert` 는 참거짓(bool) 값입니다. 이 값을 **그대로 `if` 조건문에 쓸 수 있을까요?**
*(정답은 3교시 조건문에서 확인합니다.)*""")

# 1-3 변수 이름 짓는 법 (복원)
md("""### 📖 변수 이름 잘 짓는 법

이름에는 **규칙(어기면 에러)** 과 **관습(어겨도 실행은 되지만 지키는 게 좋음)** 이 있습니다.

**규칙 — 어기면 에러가 납니다**
1. 영문·숫자·밑줄(`_`)만 사용
2. **숫자로 시작 금지** (`2fail` ✗)
3. 공백·하이픈 금지 (`fail count` ✗, `fail-count` ✗)
4. 예약어 금지 (`if`, `for`, `class` 등은 이름으로 못 씀)

**관습 — 팀·실무의 약속 (안 지켜도 실행은 됨)**
1. 파이썬 표준은 **`snake_case`** — 소문자 단어를 밑줄로 연결. 예: `failed_login_count`
2. **`is_` / `has_` / `can_`** 로 시작하면 보통 **참거짓(bool)** 값을 뜻함
3. **복수형**(`users`, `logs`)은 보통 **여러 개(리스트)** 를 뜻함
4. **안 바뀌는 값(상수)** 은 **대문자** 로. 예: `MAX_RETRY = 5`

> **왜 중요할까?** 이름만 봐도 "이건 참거짓이구나, 이건 목록이구나"를 알 수 있어 코드를 빨리 읽고 실수를 줄입니다.""")

code("""# 💻 예제 — 관습을 지킨 좋은 이름들
failed_login_count = 7          # snake_case + 의미 있는 이름
is_blocked = False              # is_ → 참거짓
users = ["kim01", "lee02"]      # 복수형 → 여러 개(리스트)
MAX_RETRY = 5                   # 대문자 → 상수
print(failed_login_count, is_blocked, users, MAX_RETRY)""")
md("""**✅ 성공 출력**
```
7 False ['kim01', 'lee02'] 5
```""")

code("""# ✍️ 문제 (출력 예측) — 변수 이름만 보고, 각 값이 어떤 종류일지 예측해 보세요.
is_online = True
error_count = 0
server_names = ["web01", "web02"]
print(is_online, error_count, server_names)   # 실행해 예측과 맞춰 보기""")

# 1-4 f-string
md("""### 📖 f-string — 값을 문장에 끼워 넣기

- 지금까지는 값을 따로따로 출력했습니다. 여러 값을 **한 문장**으로 예쁘게 출력할 때 f-string을 씁니다.
- 방법: 문자열 앞에 **`f`** 를 붙이고, 값이 들어갈 자리에 **`{변수}`** 를 적습니다.
- 로그 메시지·리포트·알림 문구에 계속 쓰는 핵심 문법입니다. 아주 쉬운 것부터 봅니다.""")

code('''# 💻 예제 — (준비) f-string 없이 그냥 출력
print("hello")''')
md("""**✅ 성공 출력**
```
hello
```""")

code('''# 💻 예제 ① — 값 하나 끼우기
name = "hanbit"
print(f"customer={name}")''')
md("""**✅ 성공 출력**
```
customer=hanbit
```""")

code('''# 💻 예제 ② — 값 두 개 끼우기
name = "hanbit"
count = 7
print(f"[{name}] fail={count}")''')
md("""**✅ 성공 출력**
```
[hanbit] fail=7
```""")

code('''# ✍️ 문제 (빈칸 채우기) — 빈칸을 채워 아래처럼 출력되게 하세요.  기대 출력: user=kim01
user = "kim01"
print(f"user={____}")''')

# ---- 심화·자습 (섹션1) ----
md("""## [심화·자습] 1 — 더 많은 예제와 문제
> 시간이 남거나 자습·리추얼 때 풀어 보세요.

### 📖 (심화) None — "값이 아직 없음"
- 자료형에는 하나가 더 있습니다: **None** ("값이 아직 없음").
- 예: 담당자가 아직 배정되지 않았을 때 `assigned_analyst = None`.""")
code("""# 💻 (심화) None
assigned_analyst = None
print(assigned_analyst, type(assigned_analyst))""")
md("""**✅ 성공 출력**
```
None <class 'NoneType'>
```""")

md("""### 📖 (심화) f-string — 계산식과 소수 자리수""")
code('''# 💻 (심화) f-string 더 보기
success = 18; fail = 7
print(f"total={success + fail}")   # 중괄호 안에서 계산도 됨
print(f"cpu={73.456:.1f}%")        # :.1f → 소수 첫째 자리''')
md("""**✅ 성공 출력**
```
total=25
cpu=73.5%
```""")

code('''# ✍️ (심화·형식 5 · 버그 수정) 한 줄만 고쳐 8 이 나오게 하세요.  기대 출력: 8
raw_value = "7"
print(raw_value + 1)   # ← 여기가 문제''')

code("""# ✍️ (심화·형식 1 · 백지 코딩테스트) 아래 정보를 알맞은 자료형 변수로 만들고 한 줄로 출력하세요.
#   · 서비스명: auth-server  · 포트: 8080  · 온라인 여부: 참
#   기대 출력:  auth-server 8080 True
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요""")

md("""### ❓ 면접 질문 1
**"파이썬에서는 왜 변수의 자료형을 미리 선언하지 않아도 되나요?"**
> 힌트: 파이썬은 값을 넣는 순간 종류를 자동으로 판단합니다. 위 개념 설명을 되짚어 한 줄로 정리해 보세요.""")

# ══════════════════════════════════════════════════
# 2. 리스트와 딕셔너리 · 80–105분
# ══════════════════════════════════════════════════
md("""---
## 2. 리스트와 딕셔너리 · 80–105분

### 📖 개념

값이 **여러 개**일 때(예: 사용자 여러 명, 로그 여러 줄) 하나씩 변수를 만들면 번거롭습니다.
여러 값을 한 번에 담는 두 가지 그릇이 있습니다.

1. **리스트 `[ ]`** — 값을 **순서대로** 담습니다.
   - 번호(인덱스)로 꺼냅니다. 번호는 **0부터** 셉니다. (`users[0]` = 첫 번째)
   - `len()` 으로 개수를 세고, `append()` 로 뒤에 추가합니다.
2. **딕셔너리 `{ }`** — **이름표(키)-값** 쌍으로 담습니다.
   - `log["user"]` 처럼 **이름으로** 꺼냅니다.
   - 보안 로그 한 줄(user·event·ip)처럼 "이름 있는 정보"에 적합합니다.""")

code("""# 💻 예제 — 리스트: 여러 사용자
users = ["kim01", "lee02", "park03"]
print(users[0])      # 첫 번째 → kim01
print(len(users))    # 개수 → 3""")
md("""**✅ 성공 출력**
```
kim01
3
```""")

code('''# 💻 예제 — 딕셔너리: 로그 한 줄
log = {"user": "kim01", "event": "login_failed"}
print(log["user"])     # 이름으로 값 꺼내기 → kim01
print(log["event"])    # → login_failed''')
md("""**✅ 성공 출력**
```
kim01
login_failed
```""")

code('''# ✍️ 문제 (출력 예측) — 무엇이 나올지 예측한 뒤 실행하세요.
users = ["kim01", "lee02", "park03"]
print(users[1])       # 예측: ?
print(len(users))     # 예측: ?''')

md("""### ❓ 확인질문

`log["event"]` 대신 `log.get("event")` 를 쓰면 **어떤 상황에서 더 안전**할까요?""")

# ---- 심화·자습 (섹션2) ----
md("""## [심화·자습] 2 — 더 많은 예제와 문제

### 📖 (심화) `.get()` 은 없는 키도 안전합니다
- 대괄호 `log["없는키"]` 는 없는 키를 꺼내면 **에러로 멈춥니다.**
- `log.get("없는키")` 는 없으면 조용히 `None` 을 돌려줍니다(멈추지 않음).
- **집합(set)·튜플(tuple)** 은 뒤 차시에서 배웁니다.""")
code('''# 💻 (심화) 대괄호 vs .get(), 그리고 append
log = {"user": "kim01"}
print(log.get("country"))   # 없으면 None
users = ["kim01"]
users.append("lee02")       # 뒤에 추가
print(users)''')
md("""**✅ 성공 출력**
```
None
['kim01', 'lee02']
```""")

code('''# ✍️ (심화·형식 1 · 백지) log 딕셔너리에서 "event" 값을 꺼내 출력하세요.
#   기대 출력:  login_failed
log = {"user": "kim01", "event": "login_failed"}
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요''')

md("""### ❓ 면접 질문 2
**"리스트와 딕셔너리의 차이는 무엇이며, 보안 로그 한 줄엔 어느 것이 더 적합한가요?"**
> 힌트: "번호로 꺼낸다 vs 이름으로 꺼낸다". 위 개념 설명에서 답을 찾아보세요.""")

# ══════════════════════════════════════════════════
# 3. 연산자와 형변환 · 105–120분
# ══════════════════════════════════════════════════
md("""---
## 3. 연산자와 형변환 · 105–120분

### 📖 개념

**(1) 연산자 — 계산하고 비교하기**
1. 산술: `+  -  *  /` (더하기·빼기·곱하기·나누기)
2. 비교: `>  <  ==  !=` — 결과는 항상 **참거짓(bool)**
3. 논리: `and  or  not` — 여러 조건을 묶기

**(2) 형변환 — 글자와 숫자 바꾸기 (실수 주의)**
- 파일(CSV)에서 읽은 숫자는 사실 **글자**입니다. 예: `"7"`
- 글자끼리는 계산이 안 되므로, `int()` 로 **진짜 숫자로 바꿔** 주어야 합니다.
- `int("7")` → `7` / `str(7)` → `"7"` / `float("3.14")` → `3.14`

> **왜 중요할까?** "글자 숫자를 안 바꿔서" 나는 오류가 실무에 아주 흔합니다.""")

code("""# 💻 예제 — 연산자 맛보기
print(7 + 3)       # 산술 → 10
print(7 > 5)       # 비교 → True
print(7 > 5 and 3 > 10)   # 논리 → False""")
md("""**✅ 성공 출력**
```
10
True
False
```""")

code('''# 💻 예제 — 형변환
raw_value = "7"          # 파일에서 읽은 값은 글자
count = int(raw_value)   # 진짜 숫자로 바꿈
print(count + 1)         # 8''')
md("""**✅ 성공 출력**
```
8
```
> `int()` 없이 `"7" + 1` 을 하면 글자+숫자라 **에러(TypeError)** 가 납니다.""")

code("""# ✍️ 문제 (출력 예측) — 결과를 예측한 뒤 실행하세요.
count = 7
print(count == 7)        # 예측: ?
print(count != 10)       # 예측: ?""")

md("""### ❓ 확인질문

`int("7건")` 처럼 숫자와 글자가 섞여 있으면 **어떤 에러**가 날까요?""")

# ---- 심화·자습 (섹션3) ----
md("""## [심화·자습] 3 — 더 많은 예제와 문제

### 📖 (심화) `=` 와 `==` 는 다릅니다
- `=` : 값을 넣어라(대입)
- `==` : 같은지 물어봐라(비교) → 결과는 참거짓""")
code("""# 💻 (심화) 비교·논리 더 보기
count = 7
print(count >= 5 and count <= 10)   # 5~10 사이? → True
print(count == 5 or count > 0)      # 둘 중 하나라도 참? → True""")
md("""**✅ 성공 출력**
```
True
True
```""")

code('''# ✍️ (심화·형식 1 · 백지) 글자 "20" 을 숫자로 바꿔 5를 더해 출력하세요.
#   기대 출력:  25
raw_value = "20"
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요''')

md('''### ❓ 면접 질문 3
**"문자열 `"7"` 과 정수 `7` 은 무엇이 다르며, 바꾸지 않으면 어떤 문제가 생기나요?"**
> 힌트: "글자끼리는 더하기가 안 된다". 위 개념 설명에서 답을 찾아보세요.''')

# ═══ 마무리 ═══
md("""---
## 오늘 요약

1. **변수** = 값을 담는 그릇(이름표). `=`는 넣기, `==`는 비교.
2. **자료형** — 문자열(str)·정수(int)·실수(float)·참거짓(bool). `type()`으로 확인. (None은 심화)
3. **이름 관습** — `snake_case`, `is_`=참거짓, 복수형=리스트, 대문자=상수.
4. **f-string** — `f"... {변수} ..."` 로 값을 문장에 끼워 출력.
5. **리스트**(순서·0부터) vs **딕셔너리**(이름표). `.get`은 없으면 None.
6. **형변환** — 파일 숫자는 글자라 `int()`로 바꿔야 계산됨.""")

md("""## 종합 퀴즈 (객관식)

**Q1.** `"kim01"` 의 자료형은? ① int ② str ③ bool
<details><summary>정답 보기</summary>② str — 따옴표로 감싼 글자이므로 문자열(str)</details>

**Q2.** `is_online = True` 의 자료형은? ① int ② bool ③ str
<details><summary>정답 보기</summary>② bool</details>

**Q3.** 파일에서 읽은 `"10"` 에 5를 더해 15를 만들려면? ① `"10" + 5` ② `int("10") + 5` ③ `str(10) + 5`
<details><summary>정답 보기</summary>② `int("10") + 5`</details>""")

md("""## 다음 시간 예고

3교시(강의2)에는 **조건문·반복문**을 배웁니다. 오늘 만든 로그를 훑으며 "실패한 로그만"
골라내는 실제 필터링으로 이어지고, 그게 오후 실습의 재료가 됩니다. 오늘도 수고했습니다.""")

# ═══ 저장 ═══
nb={"cells":cells,"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.11"}},"nbformat":4,"nbformat_minor":5}
out="/Users/hong/jm-teaching-material/26-09-ai-security-automation-bootcamp/03-samples/day01-2교시-파이썬기초-v5.ipynb"
os.makedirs(os.path.dirname(out),exist_ok=True)
with open(out,"w",encoding="utf-8") as f: json.dump(nb,f,ensure_ascii=False,indent=1)
print("cells:",len(cells)); print("saved:",out)
