# -*- coding: utf-8 -*-
# Day 1 · 2교시 파이썬 기초 교안 v4 생성 스크립트
# 규칙: 시간 충실(상세교안 분 표기) + 2레이어(본 수업 / [심화·자습]) + 코드 영어(주석 한국어)
import json, os
cells=[]
def md(t): cells.append({"cell_type":"markdown","metadata":{},"source":t})
def code(t): cells.append({"cell_type":"code","metadata":{},"execution_count":None,"outputs":[],"source":t})

# ═══ 표지 ═══
md("""# Day 1 · 2교시 — 파이썬 기초 (강의1 후반)

## 변수·자료형 · 리스트·딕셔너리 · 연산자·형변환

> **만든 날짜:** 2026-07-13 · **버전:** v4 · **범위:** 상세교안 강의1 **55–120분**

이 교안은 **상세교안의 시간표를 그대로 따릅니다.** 각 섹션 제목에 배정 시간이 적혀 있습니다.
구성은 **2단계**입니다.

- **본 수업** — 배정된 시간 안에 다루는 핵심(상세교안 그대로).
- **[심화·자습]** — 더 많은 예제·문제. **시간이 남거나, 자습·리추얼 때** 풀어 보세요.""")

# ═══ 목차 ═══
md("""## 목차

1. **변수와 자료형** · 상세교안 55–80분
2. **리스트와 딕셔너리** · 상세교안 80–105분
3. **연산자와 형변환** · 상세교안 105–120분

마무리 — 오늘 요약, 종합 퀴즈(객관식)""")

# ═══ 학습목표 ═══
md("""## 학습목표

- [ ] 변수를 만들고 자료형(int·float·str·bool)을 `type()`으로 구분하기
- [ ] **f-string**으로 값을 문장에 끼워 출력하기
- [ ] 리스트·딕셔너리로 여러 데이터를 묶고 꺼내기
- [ ] 형변환(글자↔숫자)에서 나는 오류 이해하기""")

# ═══ 오늘 만드는 것 ═══
md("""## 오늘 만드는 것

보안 관제 대상 고객사 `hanbit` 의 로그 한 줄을 **딕셔너리**로 표현하고,
**f-string**으로 `[hanbit] failed_login=7, alert=True` 같은 요약 문장을 출력합니다.
(이 감각은 오후 실습 `agent_core/day01_basic.py` 로 이어집니다.)""")

# ═══ 용어 풀이 ═══
md("""## 용어 풀이

| 용어 | 쉽게 말하면 |
|------|-------------|
| **변수** | 값에 붙이는 이름표. `x = 7` |
| **자료형** | 값의 종류(정수·실수·문자열·참거짓) |
| **int / float** | 정수 `7` / 소수점 숫자 `73.5` |
| **str / bool** | 글자 `"kim01"` / 참거짓 `True`·`False` |
| **f-string** | 문장에 값을 끼워 넣기. `f"fail {n}"` |
| **리스트 / 딕셔너리** | 순서대로 `[...]` / 이름표-값 `{키:값}` |
| **형변환** | 값의 종류 바꾸기. `"7"` → `7` |""")

# ═══ 사용법 ═══
md("""## 이 노트북 쓰는 법

- 회색 코드 칸을 클릭하고 **`Shift + Enter`** 로 **한 칸씩** 실행합니다.
- 칸의 역할 표시(5가지):

| 표시 | 뜻 |
|------|-----|
| 📖 | 개념 — 읽는 칸 |
| 💻 | 예제 — 그대로 실행해 결과 확인 |
| ✍️ | 문제 — 직접 풀어 보는 칸 |
| ✅ | 결과 — "이렇게 나오면 성공" |
| ❓ | 면접 질문 — 답은 본문 안에 있음(스스로 찾기) |

> **2단계 안내** — 각 섹션은 **본 수업**(시간 내 핵심) 다음에 **`[심화·자습]`** 부분이 옵니다.
> 심화는 시간이 남거나 자습·리추얼 때 풀면 됩니다. 본 수업만 따라가도 충분합니다.""")

# ══════════════════════════════════════════════════
# 1. 변수와 자료형 · 55–80분
# ══════════════════════════════════════════════════
md("""---
## 1. 변수와 자료형 · 상세교안 55–80분 (25분)

### 📖 개념 (본 수업)

변수는 값에 붙이는 **이름표**입니다. `이름 = 값` 으로 만들고, 그 이름으로 값을 꺼내 씁니다.
파이썬은 **자료형을 미리 선언하지 않습니다**(값을 보고 자동 판단). 기본 자료형은
`int`(정수)·`float`(실수)·`str`(문자열)·`bool`(참거짓) 네 가지이고, `type()` 으로 확인합니다.
그리고 값을 문장에 끼워 출력하는 **f-string** 을 중점적으로 익힙니다(로그·리포트에 계속 씁니다).

> **왜 `=` 가 "같다"가 아닐까?** `=` 는 "값을 넣어라(대입)", "같은지 비교"는 `==` 입니다.""")

code('''# 💻 예제 (상세교안 예제) — 변수 + f-string
customer_name = "hanbit"                 # str : 고객사 이름
failed_login_count = 7                   # int : 실패 횟수
is_alert = failed_login_count > 5        # bool: 7 > 5 → True
print(f"[{customer_name}] failed_login={failed_login_count}, alert={is_alert}")''')
md("""**✅ 성공 출력**
```
[hanbit] failed_login=7, alert=True
```""")

code("""# 💻 예제 — 자료형 4가지와 type()
port = 8080            # int
cpu_usage = 73.5       # float
user = "kim01"         # str
is_online = True       # bool
print(type(port), type(cpu_usage), type(user), type(is_online))""")
md("""**✅ 성공 출력**
```
<class 'int'> <class 'float'> <class 'str'> <class 'bool'>
```""")

code("""# ✍️ 문제 (형식 4 · 출력 예측) — 각 줄이 무엇을 출력할지 먼저 예측한 뒤 실행하세요.
score = 90
print(score >= 60)     # 예측: ?
print(type(score))     # 예측: ?""")

md("""### ❓ 확인질문 (상세교안)

`is_alert` 는 bool(True/False) 값입니다. 이 값을 **그대로 `if` 조건문에 쓸 수 있을까요?**
*(정답은 3교시 조건문에서 확인합니다.)*""")

# ---- 심화·자습 (섹션1) ----
md("""## [심화·자습] 1 — 더 많은 예제와 문제
> 시간이 남거나 자습·리추얼 때 풀어 보세요. 본 수업만 해도 괜찮습니다.

### 📖 (심화) None — "값이 아직 없음"
상세교안 범위 밖이지만 실무에서 매우 자주 나옵니다. 예: 담당자가 아직 배정되지 않음.""")
code("""# 💻 (심화) None 도 자료형입니다
assigned_analyst = None          # 아직 배정 안 됨
print(assigned_analyst, type(assigned_analyst))""")
md("""**✅ 성공 출력**
```
None <class 'NoneType'>
```""")

md("""### 📖 (심화) f-string 5단계 — 쉬운 것부터""")
code('''# 💻 (심화) f-string 여러 케이스
name = "hanbit"; n = 7; ok = True
print(f"customer={name}")                 # ① 값 하나
print(f"[{name}] fail={n}, alert={ok}")   # ② 여러 개
print(f"total={18 + n}")                  # ③ 계산식
print(f"cpu={73.456:.1f}%")               # ④ 소수 자리수''')
md("""**✅ 성공 출력**
```
customer=hanbit
[hanbit] fail=7, alert=True
total=25
cpu=73.5%
```""")

code("""# ✍️ (심화·형식 2 · 빈칸) 빈칸 ____ 을 채우세요.  기대 출력: auth-server 8080 True
service_name = "auth-server"
port = ____            # 8080 (정수)
is_online = ____       # 참
print(service_name, port, is_online)""")

code('''# ✍️ (심화·형식 5 · 버그 수정) 한 줄만 고쳐 8 이 나오게 하세요.  기대 출력: 8
raw_value = "7"
print(raw_value + 1)   # ← 여기가 문제''')

code("""# ✍️ (심화·형식 1 · 백지 코딩테스트) 아래 정보를 알맞은 자료형 변수로 만들고 한 줄로 출력하세요.
#   · 서비스명: auth-server  · 포트: 8080  · 온라인 여부: 참
#   기대 출력:  auth-server 8080 True
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요""")

md("""### ❓ 면접 질문 1
**"파이썬에서는 왜 변수의 자료형을 미리 선언하지 않아도 되나요?"**
> 답은 위 `1. 개념` 설명 안에 있습니다. "값을 보고 자동 판단"을 떠올려 보세요.""")

# ══════════════════════════════════════════════════
# 2. 리스트와 딕셔너리 · 80–105분
# ══════════════════════════════════════════════════
md("""---
## 2. 리스트와 딕셔너리 · 상세교안 80–105분 (25분)

### 📖 개념 (본 수업)

여러 값을 다룰 때 씁니다.
- **리스트 `[ ]`** — 순서 있는 값의 모음. 번호(인덱스, **0부터**)로 꺼냅니다. 대표 메서드: `append`.
- **딕셔너리 `{ }`** — 키-값 쌍. **이름으로** 꺼냅니다. 대표 메서드: `keys`, `values`, `items`.

보안 로그 한 줄은 *user·event·ip* 처럼 이름 있는 정보라 **딕셔너리**가 적합합니다.""")

code('''# 💻 예제 (상세교안 예제) — 로그 한 줄을 딕셔너리로
log_event = {
    "customer": "hanbit",
    "user": "kim01",
    "event": "login_failed",
    "ip": "203.0.113.5",
    "timestamp": "2026-09-01T09:12:00",
}
print(log_event["event"])   # 이름으로 값 꺼내기''')
md("""**✅ 성공 출력**
```
login_failed
```""")

code("""# 💻 예제 — 리스트 기본
users = ["kim01", "lee02", "park03"]
print(users[0])       # 0번째 → kim01
print(len(users))     # 개수 → 3""")
md("""**✅ 성공 출력**
```
kim01
3
```""")

code('''# ✍️ 문제 (형식 4 · 출력 예측) — 무엇이 나올지 예측한 뒤 실행하세요.
users = ["kim01", "lee02", "park03"]
print(users[1])        # 예측: ?
print(len(users))      # 예측: ?''')

md("""### ❓ 확인질문 (상세교안)

`log_event["event"]` 대신 `log_event.get("event")` 를 쓰면 **어떤 상황에서 더 안전**할까요?""")

# ---- 심화·자습 (섹션2) ----
md("""## [심화·자습] 2 — 더 많은 예제와 문제

### 📖 (심화) `.get()` 은 없는 키도 안전합니다""")
code('''# 💻 (심화) 대괄호 vs .get()
log_event = {"user": "kim01", "event": "login_failed"}
print(log_event.get("event"))     # 있으면 값
print(log_event.get("country"))   # 없으면 None (에러 없이)
users = ["kim01"]
users.append("lee02")             # 리스트에 추가
print(users)''')
md("""**✅ 성공 출력**
```
login_failed
None
['kim01', 'lee02']
```
> 대괄호 `log_event["country"]` 는 없는 키면 **에러로 멈춥니다.** `.get()` 은 `None` 을 돌려줍니다.
> **집합(set)·튜플(tuple)** 은 뒤 차시에서 배웁니다(집합은 오늘 강의2의 '중복 제거·집계').""")

code('''# ✍️ (심화·형식 1 · 백지) log_event 딕셔너리에서 "user" 값을 꺼내 출력하세요.
#   기대 출력:  kim01
log_event = {"user": "kim01", "event": "login_failed"}
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요''')

md("""### ❓ 면접 질문 2
**"리스트와 딕셔너리의 차이는 무엇이며, 보안 로그 한 줄엔 어느 것이 더 적합한가요?"**
> 답은 위 `2. 개념` 설명 안에 있습니다. "번호로 꺼낸다 vs 이름으로 꺼낸다"를 떠올려 보세요.""")

# ══════════════════════════════════════════════════
# 3. 연산자와 형변환 · 105–120분
# ══════════════════════════════════════════════════
md("""---
## 3. 연산자와 형변환 · 상세교안 105–120분 (15분)

### 📖 개념 (본 수업)

- **연산자**: 산술 `+ - * /`, 비교 `> < == !=`(결과는 bool), 논리 `and or not`.
- **형변환**: 파일(CSV)에서 읽은 숫자는 사실 **글자**입니다. `int()` 로 진짜 숫자로 바꿔야 계산됩니다.
  (`str()`/`int()`/`float()`)

> **왜 중요할까?** "글자 숫자를 안 바꿔서" 나는 오류가 실무에 아주 흔합니다.""")

code('''# 💻 예제 (상세교안 예제) — 형변환
raw_value = "7"          # 파일에서 읽은 값은 글자
count = int(raw_value)   # 진짜 숫자로 변환
print(count + 1)         # 8''')
md("""**✅ 성공 출력**
```
8
```
> `int()` 없이 `"7" + 1` 을 하면 글자+숫자라 **에러(TypeError)** 가 납니다.""")

code("""# ✍️ 문제 (형식 4 · 출력 예측) — 결과를 예측한 뒤 실행하세요.
count = 7
print(count == 7)              # 예측: ?
print(count > 5 and count < 10)  # 예측: ?""")

md("""### ❓ 확인질문 (상세교안)

`int("7건")` 처럼 숫자와 글자가 섞여 있으면 **어떤 에러**가 날까요?""")

# ---- 심화·자습 (섹션3) ----
md("""## [심화·자습] 3 — 더 많은 예제와 문제

### 📖 (심화) `=` 와 `==` 는 다릅니다
`=` 는 "값을 넣어라", `==` 는 "같은지 물어봐라". 초보가 가장 많이 헷갈립니다.""")
code("""# 💻 (심화) 비교·논리 연산 더 보기
count = 7
print(count != 7)                  # 다른가? → False
print(count >= 5 and count <= 10)  # 5~10 사이? → True""")
md("""**✅ 성공 출력**
```
False
True
```""")

code('''# ✍️ (심화·형식 1 · 백지) 글자 "20" 을 숫자로 바꿔 5를 더해 출력하세요.
#   기대 출력:  25
raw_value = "20"
# (여기에 코드를 작성하세요)
pass  # 완성한 뒤 실행하세요''')

md('''### ❓ 면접 질문 3
**"문자열 `"7"` 과 정수 `7` 은 무엇이 다르며, 바꾸지 않으면 어떤 문제가 생기나요?"**
> 답은 위 `1.`·`3. 개념` 설명 안에 있습니다. "글자끼리는 더하기가 안 된다"를 떠올려 보세요.''')

# ═══ 마무리 ═══
md("""---
## 오늘 요약

1. **변수** = 값에 붙이는 이름표. `=`는 넣기, `==`는 비교.
2. **자료형** int·float·str·bool, `type()`으로 확인. (None 은 심화)
3. **f-string** `f"... {변수} ..."` 로 값을 문장에 끼워 출력.
4. **리스트**(순서·0부터) vs **딕셔너리**(이름표, `.get`은 없으면 None).
5. **형변환** — 파일 숫자는 글자라 `int()`로 바꿔야 계산됨.""")

md("""## 종합 퀴즈 (객관식)

**Q1.** `is_online = True` 의 자료형은? ① int ② bool ③ str
<details><summary>정답 보기</summary>② bool</details>

**Q2.** `users = ["kim01", "lee02", "park03"]` 에서 두 번째 값을 꺼내려면? ① `users[0]` ② `users[1]` ③ `users[2]`
<details><summary>정답 보기</summary>② `users[1]` — 0부터 세므로 두 번째는 1번</details>

**Q3.** 파일에서 읽은 `"10"` 에 5를 더해 15를 만들려면? ① `"10" + 5` ② `int("10") + 5` ③ `str(10) + 5`
<details><summary>정답 보기</summary>② `int("10") + 5`</details>""")

md("""## 다음 시간 예고

3교시(강의2)에는 **조건문·반복문**을 배웁니다. 오늘 만든 로그 딕셔너리들을 훑으며
"실패한 로그만" 골라내는 실제 필터링으로 이어집니다. (그리고 그게 오후 실습의 재료가 됩니다.)""")

# ═══ 저장 ═══
nb={"cells":cells,"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.11"}},"nbformat":4,"nbformat_minor":5}
out="/Users/hong/jm-teaching-material/26-09-ai-security-automation-bootcamp/03-samples/day01-2교시-파이썬기초-v4.ipynb"
os.makedirs(os.path.dirname(out),exist_ok=True)
with open(out,"w",encoding="utf-8") as f: json.dump(nb,f,ensure_ascii=False,indent=1)
print("cells:",len(cells)); print("saved:",out)
