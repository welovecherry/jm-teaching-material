# -*- coding: utf-8 -*-
# Day 1 · 2교시 파이썬 기초 교안 v3 생성 스크립트
import json, os
cells=[]
def md(t): cells.append({"cell_type":"markdown","metadata":{},"source":t})
def code(t): cells.append({"cell_type":"code","metadata":{},"execution_count":None,"outputs":[],"source":t})

# ═══ 표지 ═══
md("""# Day 1 · 2교시 — 파이썬으로 첫 걸음 떼기

## 변수 · 자료형 · 리스트 · 딕셔너리 · 형변환

> **만든 날짜:** 2026-07-13 · **버전:** v3 · **예상 시간:** 약 60분
>
> **오늘의 한 줄 목표** — 보안 로그 한 줄을 파이썬으로 표현하고, 보기 좋게 출력하기

1교시에서 개발환경을 준비했습니다. 이제 코드를 한 줄씩 작성해 봅니다. 한 칸씩 천천히 따라오면 됩니다.

> 이 노트북은 **개념 → 예제 → 문제**가 반복됩니다. 예제를 실행해 눈으로 확인한 뒤,
> 바로 아래 **문제**를 직접 풀어 보면 확실히 손에 익습니다.""")

# ═══ 목차 ═══
md("""## 목차

1. **변수와 자료형** — 이름표 붙이기, 변수명 짓는 법, 자료형 5가지, f-string
2. **리스트와 딕셔너리** — 여러 값 묶기, 이름으로 꺼내기
3. **연산자와 형변환** — 계산·비교, 글자와 숫자 바꾸기

마무리 — 오늘 요약, 종합 퀴즈(객관식)""")

# ═══ 학습목표 ═══
md("""## 학습목표

이 시간이 끝나면 다음을 할 수 있습니다.

- [ ] 변수를 만들고, **관습에 맞는 이름**을 붙이기
- [ ] 자료형 5가지(정수·실수·문자열·참거짓·**None**)를 구분하기
- [ ] f-string으로 값을 문장에 끼워 출력하기
- [ ] 리스트·딕셔너리로 여러 데이터를 묶고 꺼내기
- [ ] 형변환(글자↔숫자)에서 나는 오류를 이해하기""")

# ═══ 오늘 만드는 것 ═══
md("""## 오늘 만드는 것

보안 관제 대상 고객사 **`hanbit`** 의 로그 한 줄을 **딕셔너리**로 표현하고,
**f-string**으로 `[hanbit] failed_login=7, alert=True` 같은 요약 문장을 출력합니다.
이 결과물은 이후 실습 과제(`day01_basic.py`, 로그 필터링)의 재료가 됩니다.""")

# ═══ 용어 풀이 ═══
md("""## 용어 풀이

| 용어 | 쉽게 말하면 |
|------|-------------|
| **변수(variable)** | 값에 붙이는 이름표. `x = 7` 은 "값 7에 x라는 이름표를 붙임" |
| **자료형(type)** | 값의 종류(정수·실수·문자열·참거짓·값없음) |
| **정수 `int`** | 소수점 없는 숫자. 예: `7` |
| **실수 `float`** | 소수점 있는 숫자. 예: `73.5` |
| **문자열 `str`** | 글자(따옴표로 감쌈). 예: `"kim01"` |
| **참거짓 `bool`** | `True` 또는 `False` |
| **None** | "값이 아직 없음". 예: 담당자 미배정 |
| **snake_case** | 파이썬 변수명 관습. 단어를 밑줄로 연결(`failed_login_count`) |
| **f-string** | 문장에 값을 끼워 넣는 출력 방법. `f"fail {n}"` |
| **리스트 `list`** | 여러 값을 순서대로 담는 상자 `[...]` |
| **딕셔너리 `dict`** | 이름표-값 쌍으로 담는 상자 `{키: 값}` |
| **형변환** | 값의 종류 바꾸기. 글자 `"7"` → 숫자 `7` |""")

# ═══ 사용법 ═══
md("""## 이 노트북 사용법

- 회색 코드 칸을 클릭하고 **`Shift + Enter`** 로 실행합니다. **한 칸씩** 순서대로.
- 칸의 역할은 5가지 표시로 구분합니다.

| 표시 | 뜻 |
|------|-----|
| 📖 | 개념 — 읽는 칸 |
| 💻 | 예제 — 그대로 실행해 결과를 확인하는 칸 |
| ✍️ | 문제 — 예제를 조금 바꿔 직접 풀어 보는 칸 |
| ✅ | 결과 — "이렇게 나오면 성공"인 예상 출력 |
| ❓ | 면접 질문 — **답은 위 본문 안에** 있습니다. 스스로 찾아보세요 |

> **면접 질문(❓) 활용법** — 먼저 스스로 답을 떠올려 적고, 막히면 **위 개념 설명을 다시 읽어**
> 답을 찾습니다. 필요하면 AI에게 물어봐도 좋습니다. (모든 질문은 오늘 배운 범위 안에서 답할 수 있습니다.)""")

# ══════════════════════════════════════════
# 1. 변수와 자료형
# ══════════════════════════════════════════
# 1-1 변수란
md("""---
## 1. 변수와 자료형

### 1-1. 변수는 값에 붙이는 '이름표'입니다

프로그램은 값을 계속 기억해야 합니다. 그래서 값에 이름표를 붙입니다. 이것이 변수입니다.
`이름 = 값` 으로 쓰면, 그 이름으로 값을 다시 꺼내 씁니다.

> **왜 `=` 가 "같다"가 아닐까?** 파이썬에서 `=` 는 "**값을 넣어라(대입)**"라는 뜻입니다.
> "같은지 비교"는 `==`(등호 두 개)로 따로 씁니다. 이 둘을 섞으면 초보자가 가장 많이 헷갈립니다.

> **왜 한 줄에 변수 하나씩?** 값이 틀렸을 때 어느 줄이 문제인지 바로 보이기 때문입니다.""")

code("""# 💻 예제 — 변수를 만들고, 값을 바꾸기(재할당)
customer_name = "hanbit"   # "hanbit" 값에 customer_name 이라는 이름표를 붙임
print(customer_name)       # hanbit

customer_name = "daum"     # 같은 변수에 새 값을 넣으면 값이 바뀜(재할당)
print(customer_name)       # daum""")
md("""**✅ 성공 출력**
```
hanbit
daum
```""")

code("""# ✍️ 문제 — 위 예제처럼 변수 하나를 만들고, 다른 값으로 한 번 바꿔 출력해 보세요.
#   목표: 아무 값이나 넣어 두 줄이 서로 다르게 출력되면 성공.
analyst_name = "kim01"     # 시작 값
print(analyst_name)
# 아래 두 줄의 주석(#)을 지우고, analyst_name 에 다른 값을 넣어 출력해 보세요.
# analyst_name = "..."
# print(analyst_name)""")

# 1-2 변수 이름 짓는 법
md("""### 1-2. 변수 이름 짓는 법

이름에는 **규칙(어기면 에러)** 과 **관습(어겨도 실행은 되지만 지키는 게 좋음)** 이 있습니다.

**규칙 — 어기면 에러가 납니다**
- 영문·숫자·밑줄(`_`)만 사용, **숫자로 시작 금지** (`2fail` ✗)
- 공백·하이픈 금지 (`fail count` ✗, `fail-count` ✗)
- 예약어 금지 (`if`, `for`, `class` 등은 이름으로 못 씀)

**관습 — 팀·실무의 약속**
- 파이썬 표준은 **`snake_case`** (`failed_login_count`). 참고로 다른 언어는 `camelCase`,
  `PascalCase`도 쓰지만, **파이썬에선 snake_case** 를 씁니다.
- **`is_` / `has_` / `can_`** 로 시작하면 보통 **참거짓(bool)** 값을 뜻합니다.
- **복수형**(`users`, `logs`)은 보통 **여러 개(리스트)** 를 뜻합니다.
- **안 바뀌는 값(상수)** 은 **대문자**로 (`MAX_RETRY = 5`).

> **왜 이름 규칙이 중요할까?** 이름만 봐도 "이건 참거짓이구나, 이건 목록이구나"를 알 수 있어
> 코드를 훨씬 빨리 읽고, 실수를 줄입니다.""")

code("""# 💻 예제 — 관습을 지킨 좋은 이름들
failed_login_count = 7                 # snake_case + 의미 있는 이름
is_blocked = False                     # is_ → 참거짓(bool)
has_permission = True                  # has_ → 참거짓(bool)
users = ["kim01", "lee02"]             # 복수형 → 여러 개(리스트)
MAX_RETRY = 5                          # 대문자 → 상수(안 바뀌는 값)

print(failed_login_count, is_blocked, has_permission, users, MAX_RETRY)""")
md("""**✅ 성공 출력**
```
7 False True ['kim01', 'lee02'] 5
```
> **주의(어기면 에러):** `2fail = 1`(숫자 시작), `fail-count = 1`(하이픈), `class = 1`(예약어)은
> 모두 에러입니다. `camelCase`는 에러는 아니지만 파이썬 관습이 아닙니다.""")

code("""# ✍️ 문제 — 관습에 맞는 이름을 직접 붙여 보세요. (예제의 이름 규칙을 참고)
#   (1) '차단 여부(참/거짓)' 값 True   → is_ 로 시작하는 이름
#   (2) '최대 접속 시도 3'             → 상수처럼 대문자
#   목표 출력:  True 3
# 아래 ??? 를 알맞은 변수명으로 바꾸고 주석(#)을 지워 실행해 보세요.
# ??? = True
# ??? = 3
# print(???, ???)
pass  # 위 주석대로 완성한 뒤 실행하세요""")

# 1-3 자료형 5가지
md("""### 1-3. 자료형 5가지와 `type()`

값에는 종류(자료형)가 있습니다. `type(값)` 으로 확인합니다.

> **왜 파이썬은 자료형을 미리 안 적을까?** 어떤 언어는 `int x = 7` 처럼 종류를 먼저 선언합니다.
> 파이썬은 값을 넣는 순간 종류를 **자동으로 판단**합니다(동적 타이핑). 그래서 시작이 쉽습니다.""")

code("""# 💻 예제 — 자료형 5가지를 모두 만들어 봅니다 (보안 관제 상황)
failed_login_count = 7          # int   : 정수
cpu_usage = 73.5                # float : 소수점 숫자
user = "kim01"                  # str   : 문자열
is_alert = True                 # bool  : 참거짓
assigned_analyst = None         # None  : 담당자 아직 미배정("값 없음")

print(failed_login_count, cpu_usage, user, is_alert, assigned_analyst)""")
md("""**✅ 성공 출력**
```
7 73.5 kim01 True None
```""")

code("""# 💻 예제 — 각 값의 '종류'를 확인합니다
print(type(failed_login_count))   # int
print(type(cpu_usage))            # float
print(type(user))                 # str
print(type(is_alert))             # bool
print(type(assigned_analyst))     # NoneType""")
md("""**✅ 성공 출력**
```
<class 'int'>
<class 'float'>
<class 'str'>
<class 'bool'>
<class 'NoneType'>
```
> **None 이란?** "값이 아직 없음"을 나타냅니다. 예: 담당자가 아직 정해지지 않음.
> 뒤에서 딕셔너리 `.get()` 이 없는 값을 찾을 때도 `None` 이 나옵니다.""")

code("""# ✍️ 문제 — 상황에 맞는 자료형으로 변수를 만들어 보세요.
#   (1) 응답 시간 0.23초        → 실수(float)
#   (2) 아직 배정 안 된 담당자   → None
#   목표 출력:  0.23 None
# response_time = ...
# assigned_analyst = ...
# print(response_time, assigned_analyst)
pass  # 위 주석대로 완성한 뒤 실행하세요""")

# 1-4 f-string
md("""### 1-4. f-string — 값을 문장에 끼워 넣기

문자열 앞에 **`f`** 를 붙이고, 값 자리에 **`{변수}`** 를 적습니다.

> **왜 f-string을 배울까?** 로그 메시지·리포트·알림 문구에 계속 씁니다. 쉬운 것부터 5단계로 늘려 봅니다.""")

code('''# 💻 예제 ① — 값 하나 끼우기
customer_name = "hanbit"
print(f"customer={customer_name}")''')
md("""**✅ 성공 출력**
```
customer=hanbit
```""")

code('''# 💻 예제 ② — 숫자 끼우기
failed_login_count = 7
print(f"failed_login={failed_login_count}")''')
md("""**✅ 성공 출력**
```
failed_login=7
```""")

code('''# 💻 예제 ③ — 여러 값을 한 문장에
customer_name = "hanbit"
failed_login_count = 7
is_alert = True
print(f"[{customer_name}] failed_login={failed_login_count}, alert={is_alert}")''')
md("""**✅ 성공 출력**
```
[hanbit] failed_login=7, alert=True
```""")

code('''# 💻 예제 ④ — 중괄호 안에서 계산도 됩니다
success = 18
fail = 7
print(f"total={success + fail}")''')
md("""**✅ 성공 출력**
```
total=25
```""")

code('''# 💻 예제 ⑤ — 소수점 자리수 지정
cpu_usage = 73.456
print(f"cpu={cpu_usage:.1f}%")   # :.1f = 소수점 첫째 자리''')
md("""**✅ 성공 출력**
```
cpu=73.5%
```""")

code('''# ✍️ 문제 — 예제 ③을 참고해, 아래 값으로 로그 요약 문장을 만들어 보세요.
#   목표 출력:  [daum] user=lee02, blocked=True
customer_name = "daum"
user = "lee02"
is_blocked = True
# print(f"...")  # ← 여기를 완성해 보세요
pass  # 위 주석대로 완성한 뒤 실행하세요''')

# ❓ 면접 질문 1 (답 없음)
md("""### ❓ 면접 질문 1

**"파이썬에서는 왜 변수의 자료형(int/str 등)을 미리 선언하지 않아도 되나요?"**

*(오늘 배운 '자료형' 범위 안에서 답할 수 있습니다.)*

> ✍️ 내 답:
>
> ✍️ 내 말로 한 줄 요약:

> **답은 위 `1-3` 개념 설명 안에 있습니다.** 막히면 "왜 파이썬은 자료형을 미리 안 적을까?"
> 콜아웃을 다시 읽어 보세요.""")

# ══════════════════════════════════════════
# 2. 리스트와 딕셔너리
# ══════════════════════════════════════════
md("""---
## 2. 리스트와 딕셔너리

### 2-1. 리스트 `[ ]` — 여러 값을 '순서대로' 담기

값이 여러 개일 때(예: 사용자 목록) 리스트에 순서대로 담습니다. 대괄호 `[ ]` 를 씁니다.
(1-2에서 본 "복수형 이름"이 보통 리스트입니다.)

> **왜 번호(인덱스)로 꺼낼까?** 리스트는 "몇 번째"가 중요하기 때문입니다. 번호는 **0부터** 셉니다.""")

code("""# 💻 예제 — 리스트 만들기·꺼내기·개수·추가
users = ["kim01", "lee02", "park03"]
print(users[0])          # 0번째 값 → kim01
print(len(users))        # 개수 → 3
users.append("choi04")   # 맨 뒤에 추가
print(users)""")
md("""**✅ 성공 출력**
```
kim01
3
['kim01', 'lee02', 'park03', 'choi04']
```
> **주의:** 첫 번째는 `users[0]`, 두 번째는 `users[1]` 입니다.""")

code("""# ✍️ 문제 — 예제를 참고해 아래를 해보세요.
#   (1) users 의 '두 번째' 값을 출력  (목표: lee02)
#   (2) "yoon05" 를 추가한 뒤 개수 출력 (목표: 4)
users = ["kim01", "lee02", "park03"]
# print(...)          # 두 번째 값
# users.append(...)   # 추가
# print(len(users))   # 개수
pass  # 위 주석대로 완성한 뒤 실행하세요""")

md("""### 2-2. 딕셔너리 `{ }` — '이름표: 값' 쌍으로 담기

보안 로그 한 줄은 *user·event·ip* 처럼 **이름 있는 정보**의 묶음이라 딕셔너리가 적합합니다.

> **왜 리스트 대신 딕셔너리?** "몇 번째"가 아니라 "무슨 항목"이 중요할 때 씁니다.
> `log["user"]` 가 `log[0]` 보다 뜻이 분명합니다.

> **집합(set)·튜플(tuple)** 은 오늘은 다루지 않습니다. 집합은 뒤(반복문 뒤 '중복 제거·집계')에서 배웁니다.""")

code('''# 💻 예제 — 로그 한 줄을 딕셔너리로 + 안전하게 꺼내기(.get)
log_event = {"customer": "hanbit", "user": "kim01", "event": "login_failed"}
print(log_event["event"])       # 이름으로 값 꺼내기 → login_failed
print(log_event.get("country")) # 없는 이름 → None (에러 없이 넘어감)''')
md("""**✅ 성공 출력**
```
login_failed
None
```
> **주의:** `log_event["country"]`(대괄호)로 없는 이름을 꺼내면 **에러로 멈춥니다.**
> `.get()` 은 없으면 `None` 을 돌려주어 멈추지 않습니다. (1-3의 None 과 연결됩니다.)""")

code('''# ✍️ 문제 — 예제를 참고해 아래를 해보세요.
#   (1) log_event 에서 "user" 값을 출력   (목표: kim01)
#   (2) .get 으로 없는 "ip" 를 꺼내 출력   (목표: None)
log_event = {"customer": "hanbit", "user": "kim01", "event": "login_failed"}
# print(...)
# print(...)
pass  # 위 주석대로 완성한 뒤 실행하세요''')

# ❓ 면접 질문 2
md("""### ❓ 면접 질문 2

**"리스트와 딕셔너리의 차이는 무엇이며, 보안 로그 한 줄을 표현할 땐 어느 것이 더 적합한가요?"**

*(오늘 배운 '리스트/딕셔너리' 범위 안에서 답할 수 있습니다.)*

> ✍️ 내 답:
>
> ✍️ 내 말로 한 줄 요약:

> **답은 위 `2-1`·`2-2` 개념 설명 안에 있습니다.** "번호로 꺼낸다 vs 이름으로 꺼낸다"를 떠올려 보세요.""")

# ══════════════════════════════════════════
# 3. 연산자와 형변환
# ══════════════════════════════════════════
md("""---
## 3. 연산자와 형변환

### 3-1. 연산자 — 계산·비교·판단

- **산술** `+ - * /`
- **비교** `> < >= <= == !=` → 결과는 참거짓(bool)
- **논리** `and or not`

> **왜 `==` 는 등호가 두 개일까?** `=` 는 "값을 넣어라(대입)", `==` 는 "같은지 물어봐라(비교)"
> 로 뜻이 완전히 다릅니다.""")

code("""# 💻 예제 — 세 종류 연산자
print(7 + 3)             # 산술 → 10
print(7 == 7)            # 비교 → True
print(7 > 5 and 3 > 10)  # 논리 → True and False → False""")
md("""**✅ 성공 출력**
```
10
True
False
```
`and` 는 둘 다 참이어야 참입니다.""")

code("""# ✍️ 문제 — 예제를 참고해 아래 결과를 예상하고, 실행해 확인해 보세요.
#   목표: 각 줄이 무엇을 출력할지 먼저 종이에 적어 본 뒤 실행
count = 7
print(count != 7)        # 다른가?
print(count >= 5)        # 5 이상인가?
print(count == 5 or count > 0)  # 둘 중 하나라도 참인가?""")

md("""### 3-2. 형변환 — 글자와 숫자 바꾸기 (실수 주의 구간)

파일(CSV)에서 읽은 숫자는 사실 **글자**입니다. `int()` 로 진짜 숫자로 바꿔야 계산됩니다.

> **왜 중요할까?** 실무 오류의 상당수가 "글자 숫자를 안 바꿔서" 생깁니다.""")

code('''# 💻 예제 — 글자를 숫자로 바꿔 계산
raw_value = "7"          # 파일에서 읽은 값은 글자인 경우가 많음
count = int(raw_value)   # int() 로 진짜 숫자로 변환
print(count + 1)         # 8''')
md("""**✅ 성공 출력**
```
8
```
> **주의:** `int()` 없이 `"7" + 1` 을 하면 글자+숫자라 **에러(TypeError)** 가 납니다.""")

code('''# ✍️ 문제 — 예제를 참고해, 글자 "20" 을 숫자로 바꿔 5를 더해 보세요.
#   목표 출력:  25
raw_value = "20"
# count = int(...)
# print(...)
pass  # 위 주석대로 완성한 뒤 실행하세요''')

# ❓ 면접 질문 3
md('''### ❓ 면접 질문 3

**"문자열 `"7"` 과 정수 `7` 은 무엇이 다르며, 문자열을 숫자로 바꾸지 않으면 어떤 문제가 생기나요?"**

*(오늘 배운 '자료형·형변환' 범위 안에서 답할 수 있습니다.)*

> ✍️ 내 답:
>
> ✍️ 내 말로 한 줄 요약:

> **답은 위 `1-3`·`3-2` 개념 설명 안에 있습니다.** "글자끼리는 더하기가 안 된다"를 떠올려 보세요.''')

# ═══ 마무리 ═══
md("""---
## 오늘 요약

1. **변수** = 값에 붙이는 이름표. `=` 는 "넣기", `==` 는 "비교".
2. **이름 관습** — `snake_case`, `is_`=참거짓, 복수형=리스트, 대문자=상수.
3. **자료형 5가지** — int·float·str·bool·**None**(값 없음). `type()` 으로 확인.
4. **f-string** — `f"... {변수} ..."`, 중괄호 안에 계산식·소수 포맷도 가능.
5. **리스트**(순서·0부터) vs **딕셔너리**(이름표, `.get` 은 없으면 None).
6. **형변환** — 파일 숫자는 글자라 `int()` 로 바꿔야 계산됨.""")

md("""## 종합 퀴즈 (객관식)

**Q1.** `is_blocked = False` 의 자료형은?
① int ② bool ③ str
<details><summary>정답 보기</summary>② bool — `is_` 로 시작하고 값이 True/False 이므로 참거짓(bool).</details>

**Q2.** 담당자가 아직 정해지지 않았음을 나타내는 값으로 알맞은 것은?
① `0` ② `""` ③ `None`
<details><summary>정답 보기</summary>③ `None` — "값이 아직 없음"을 뜻함.</details>

**Q3.** 파일에서 읽은 `"10"` 에 5를 더해 15를 만들려면?
① `"10" + 5` ② `int("10") + 5` ③ `str(10) + 5`
<details><summary>정답 보기</summary>② `int("10") + 5` — 먼저 int() 로 진짜 숫자로 바꾼 뒤 더함.</details>""")

md("""## 다음 시간 예고

3교시에는 **조건문(`if`)과 반복문(`for`)** 을 배웁니다. 오늘 만든 로그 딕셔너리들을 훑으며
"실패한 로그만" 골라내는 실제 필터링을 해봅니다. 오늘도 수고했습니다.""")

# ═══ 저장 ═══
nb={"cells":cells,"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.11"}},"nbformat":4,"nbformat_minor":5}
out="/Users/hong/jm-teaching-material/26-09-ai-security-automation-bootcamp/03-samples/day01-2교시-파이썬기초-v3.ipynb"
os.makedirs(os.path.dirname(out),exist_ok=True)
with open(out,"w",encoding="utf-8") as f: json.dump(nb,f,ensure_ascii=False,indent=1)
print("cells:",len(cells)); print("saved:",out)
