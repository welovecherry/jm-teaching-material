# -*- coding: utf-8 -*-
import json, os

cells = []
def md(text): cells.append({"cell_type":"markdown","metadata":{},"source":text})
def code(text): cells.append({"cell_type":"code","metadata":{},"execution_count":None,"outputs":[],"source":text})

# ── 표지 ──────────────────────────────────────────────
md("""# Day 1 · 2교시 — 파이썬으로 첫 걸음 떼기 🐍
### 변수 · 자료형 · 리스트 · 딕셔너리 · 형변환

> ⏱️ **약 60분** &nbsp;|&nbsp; 🧩 **난이도:** 완전 처음
>
> 🎯 **오늘의 한 줄 목표** — *"보안 로그 한 줄을 파이썬으로 표현하고, 화면에 보기 좋게 출력하기"*

1교시에서 개발환경과 프로젝트 폴더를 준비했죠. 이제 진짜 **코드를 한 줄씩** 써봅니다.
어렵지 않아요. 천천히, 한 칸씩 같이 가봅시다. 🙌""")

# ── 목차 ──────────────────────────────────────────────
md("""## 📑 오늘 갈 길 (목차)

1. **변수와 기본 자료형** — 값에 이름표 붙이기, 숫자·글자·참거짓 구분하기
2. **리스트와 딕셔너리** — 여러 값을 묶고, 이름으로 꺼내기
3. **연산자와 형변환** — 계산하기, 글자↔숫자 바꾸기 (여기서 실수 많이 나요 ⚠️)

마무리로 **오늘 요약 + 퀴즈 3문항**이 있어요.""")

# ── 학습목표 ───────────────────────────────────────────
md("""## 🎯 이 시간이 끝나면 할 수 있어요

- [ ] 변수에 값을 담고, **4가지 기본 자료형**(정수·실수·문자열·참거짓)을 구분한다
- [ ] **f-string**으로 값을 문장에 끼워 읽기 좋게 출력한다
- [ ] **리스트**와 **딕셔너리**로 여러 데이터를 묶고 꺼낸다
- [ ] **형변환**(글자↔숫자)에서 자주 나는 오류를 이해한다""")

# ── 오늘의 산출물 ──────────────────────────────────────
md("""## 📦 오늘 만드는 것

> 보안 로그 한 줄(누가, 언제, 무슨 이벤트를 일으켰나)을 **딕셔너리**로 표현하고,
> **f-string**으로 *"A사 고객사 - 실패 로그인 7건, 경보여부: True"* 같은 요약 문장을 출력합니다.

이건 나중에 실습 과제(`day01_basic.py`, 로그 필터링 스크립트)의 **재료**가 됩니다. 🧱""")

# ── 용어 풀이 ─────────────────────────────────────────
md("""## 📖 오늘 처음 나오는 말, 미리 풀어요

| 용어 | 쉽게 말하면 |
|------|-------------|
| **변수(variable)** | 값에 붙이는 **이름표**. `x = 7` 은 "7이라는 값에 x라는 이름표를 붙였다"는 뜻 |
| **자료형(type)** | 값의 **종류**. 숫자인지 글자인지 참/거짓인지 |
| **정수 `int`** | 소수점 없는 숫자. 예: `7`, `-3`, `0` |
| **실수 `float`** | 소수점 있는 숫자. 예: `3.14`, `0.5` |
| **문자열 `str`** | 글자(따옴표로 감쌈). 예: `'kim01'`, `"로그인 실패"` |
| **참거짓 `bool`** | 참(`True`)이거나 거짓(`False`) 둘 중 하나 |
| **f-string** | 문장 안에 값을 끼워 넣는 편한 출력 방법. `f'실패 {n}건'` |
| **리스트 `list`** | 여러 값을 **순서대로** 담는 상자 `[...]` |
| **딕셔너리 `dict`** | **이름표-값** 쌍으로 담는 상자 `{키: 값}` |
| **형변환** | 값의 종류를 바꾸는 것. 글자 `'7'` → 숫자 `7` |

> 지금 다 외우지 않아도 돼요. 아래서 하나씩 직접 써보면 자연스럽게 익혀집니다. 😉""")

# ── 사용법 안내 ────────────────────────────────────────
md("""## 🕹️ 이 노트북 쓰는 법 (30초 읽기)

- 코드 칸(회색 칸)을 클릭하고 **`Shift + Enter`** 를 누르면 그 칸이 실행돼요.
- **한 칸씩 순서대로** 실행하세요. (한 번에 몰아 실행하지 말고, 하나 실행 → 결과 확인 → 다음)
- 칸마다 이모지로 역할을 표시했어요:
  - 📌 **개념** — 읽는 칸  💻 **따라치기** — 그대로 실행해보는 칸
  - ✍️ **직접** — 여러분이 값을 바꿔보는 칸  📝 **메모** — 자유롭게 적는 칸
  - ❓ **확인질문** — 잠깐 생각해보는 칸
- 실행하면 **"✅ 이렇게 나오면 성공"** 안내가 바로 아래 있으니 결과를 비교하세요.""")

# ══ 섹션 1: 변수와 기본 자료형 ═══════════════════════════
md("""---
## 1️⃣ 변수와 기본 자료형

### 📌 변수 = 값에 붙이는 '이름표'

프로그램은 값을 계속 기억해야 해요. 그래서 값에 **이름표**를 붙입니다. 이게 변수예요.

택배 상자에 "김철수" 라고 이름표를 붙이면 나중에 그 이름으로 상자를 찾죠?
파이썬도 똑같이 `이름 = 값` 이라고 쓰면, 그 이름으로 값을 꺼내 쓸 수 있어요.

> 💡 파이썬은 **자료형을 미리 말해줄 필요가 없어요.** 그냥 `= 값` 하면 파이썬이 알아서 종류를 판단합니다.""")

code("""# 💻 따라치기 — 변수 3개를 만들어 봅시다.
customer_name = 'A사'          # 문자열(str): 글자는 따옴표로 감싼다
failed_login_count = 7         # 정수(int): 소수점 없는 숫자
is_alert = failed_login_count > 5   # 참거짓(bool): 7 > 5 는 참이므로 True

# print()는 () 안의 값을 화면에 보여주는 명령이에요.
print(customer_name)
print(failed_login_count)
print(is_alert)""")

md("""**✅ 이렇게 나오면 성공**
```
A사
7
True
```
`failed_login_count > 5` 는 "7이 5보다 큰가?" 라는 질문이고, 답이 참이라 `True`가 담겼어요.""")

md("""### 📌 값의 '종류' 4가지 — `type()`으로 확인하기

값은 종류(자료형)가 있어요. `type(값)` 을 쓰면 그 종류를 알려줍니다.""")

code("""# 💻 따라치기 — 각 값의 종류를 확인해봅시다.
print(type('A사'))    # 글자 → str
print(type(7))        # 정수 → int
print(type(3.14))     # 소수점 숫자 → float
print(type(True))     # 참/거짓 → bool""")

md("""**✅ 이렇게 나오면 성공**
```
<class 'str'>
<class 'int'>
<class 'float'>
<class 'bool'>
```
`<class 'str'>` 은 "이 값은 str(문자열) 종류다" 라는 뜻이에요.""")

md("""### 📌 f-string — 값을 문장에 끼워 넣기

값 여러 개를 한 문장으로 예쁘게 출력하고 싶을 때 씁니다.
문자열 앞에 **`f`** 를 붙이고, 값이 들어갈 자리에 **`{변수이름}`** 을 적으면 돼요.

> 이 문법은 앞으로 로그 메시지, 리포트 만들 때 **계속** 나옵니다. 오늘 확실히 익혀두면 최고예요. ⭐""")

code("""# 💻 따라치기 — f-string으로 한 문장 만들기
customer_name = 'A사'
failed_login_count = 7
is_alert = failed_login_count > 5

# {} 안에 변수 이름을 넣으면 그 값으로 바뀝니다.
print(f'{customer_name} 고객사 - 실패 로그인 {failed_login_count}건, 경보여부: {is_alert}')""")

md("""**✅ 이렇게 나오면 성공**
```
A사 고객사 - 실패 로그인 7건, 경보여부: True
```
`{customer_name}` 자리에 `A사`, `{failed_login_count}` 자리에 `7` 이 쏙 들어갔죠?""")

code("""# ✍️ 직접 바꿔보기 — 아래 값을 '여러분 것'으로 바꿔서 실행해보세요.
#    (예: 회사 이름을 'B사'로, 실패 횟수를 12로)

customer_name = 'A사'          # ← 원하는 회사 이름으로 바꿔보세요
failed_login_count = 7         # ← 원하는 숫자로 바꿔보세요
is_alert = failed_login_count > 5

print(f'{customer_name} 고객사 - 실패 로그인 {failed_login_count}건, 경보여부: {is_alert}')""")

md("""### 📝 메모 (이 칸을 더블클릭해서 자유롭게 적으세요)

- 내가 바꿔본 값:
- 헷갈렸던 점:""")

md("""### ❓ 확인질문

`is_alert` 는 `True` 또는 `False`(bool) 값이에요.
이 값을 나중에 **`if` 조건문**에 그대로 쓸 수 있을까요? 왜 그럴까요?

<details><summary>🤔 힌트 보기</summary>

`if` 는 "참이면 실행" 하는 명령이에요. bool은 이미 참/거짓 그 자체라서... 어떨까요? (정답은 3교시에!)
</details>""")

# ══ 섹션 2: 리스트와 딕셔너리 ════════════════════════════
md("""---
## 2️⃣ 리스트와 딕셔너리

### 📌 리스트 `[ ]` — 여러 값을 '순서대로' 담기

값이 하나가 아니라 여러 개일 때(예: 로그 여러 줄) 하나하나 변수를 만들면 힘들어요.
**리스트**는 여러 값을 **순서대로** 한 상자에 담습니다. 대괄호 `[ ]` 를 써요.""")

code("""# 💻 따라치기 — 사용자 이름 3개를 리스트에 담기
users = ['kim01', 'lee02', 'park03']   # 값들을 쉼표로 구분해 [] 안에 넣는다

print(users)         # 리스트 전체
print(users[0])      # 0번째 값 (파이썬은 0부터 센다!)
print(len(users))    # len()은 개수를 세어준다""")

md("""**✅ 이렇게 나오면 성공**
```
['kim01', 'lee02', 'park03']
kim01
3
```
> ⚠️ **자주 하는 실수:** 파이썬은 순서를 **0부터** 셉니다. 첫 번째 값은 `users[0]` 이에요. `users[1]` 은 두 번째!""")

md("""### 📌 딕셔너리 `{ }` — '이름표: 값' 쌍으로 담기

리스트는 순서(0, 1, 2...)로 값을 꺼내죠. 그런데 보안 로그 한 줄에는
*누가·언제·무슨 이벤트·어느 IP* 처럼 **이름이 있는 정보**가 많아요.

이럴 땐 **딕셔너리**가 딱이에요. `{이름표(키): 값}` 형태로 담고, **이름으로** 값을 꺼냅니다.""")

code("""# 💻 따라치기 — 보안 로그 한 줄을 딕셔너리로 표현하기
log_event = {
    'customer': 'A사',
    'user': 'kim01',
    'event': 'login_failed',
    'ip': '203.0.113.5',
    'timestamp': '2026-09-01T09:12:00'
}

print(log_event)              # 딕셔너리 전체
print(log_event['event'])     # 'event' 라는 이름표의 값만 꺼내기""")

md("""**✅ 이렇게 나오면 성공**
```
{'customer': 'A사', 'user': 'kim01', 'event': 'login_failed', 'ip': '203.0.113.5', 'timestamp': '2026-09-01T09:12:00'}
login_failed
```

> 💡 **더 안전한 꺼내기 — `.get()`**
> `log_event['없는이름']` 처럼 없는 이름표를 꺼내면 **에러가 나서 프로그램이 멈춰요.**
> `log_event.get('없는이름')` 은 없으면 조용히 `None`(값 없음)을 돌려줘서 안 멈춥니다.""")

code("""# ✍️ 직접 바꿔보기 — 여러분의 로그 한 줄을 만들어보세요.
#    user 를 여러분 아이디로, event 를 'login_success' 로 바꿔서 출력해보기

my_log = {
    'user': 'kim01',           # ← 바꿔보세요
    'event': 'login_failed',   # ← 'login_success' 로 바꿔보세요
}

print(f"사용자 {my_log['user']} 의 이벤트: {my_log['event']}")""")

md("""### 📝 메모

- 리스트와 딕셔너리, 각각 언제 쓰면 좋을지 내 말로 적어보기:""")

md("""### ❓ 확인질문

`log_event['event']` 대신 `log_event.get('event')` 를 쓰면 **어떤 상황에서 더 안전**할까요?

<details><summary>🤔 힌트 보기</summary>

이름표가 **없을 때** 두 방식의 행동이 달라요. 하나는 멈추고, 하나는...?
</details>""")

# ══ 섹션 3: 연산자와 형변환 ═════════════════════════════
md("""---
## 3️⃣ 연산자와 형변환

### 📌 연산자 — 계산하고, 비교하고, 판단하기

- **산술** `+ - * /` : 더하기·빼기·곱하기·나누기
- **비교** `> < >= <= == !=` : 크다·작다·같다·다르다 → 결과는 **참/거짓(bool)**
- **논리** `and or not` : 여러 조건을 묶기""")

code("""# 💻 따라치기 — 세 가지 연산자를 맛보기
print(7 + 3)          # 산술: 10
print(7 > 5)          # 비교: True (7이 5보다 크다)
print(7 > 5 and 3 > 10)   # 논리: True 그리고 False → False""")

md("""**✅ 이렇게 나오면 성공**
```
10
True
False
```
`and` 는 **둘 다 참**이어야 참이에요. `3 > 10` 이 거짓이라 전체가 `False`가 됐어요.""")

md("""### 📌 형변환 — 글자 ↔ 숫자 바꾸기 (⚠️ 실수 주의 구간)

파일(CSV)에서 숫자를 읽어오면, 사실은 **숫자처럼 생긴 '글자'** 로 들어와요. (`'7'` 은 숫자 7이 아니라 글자!)
글자끼리는 더하기가 안 되니, `int()` 로 **진짜 숫자로 바꿔줘야** 계산돼요.

- `int('7')` → 숫자 `7`  ·  `str(7)` → 글자 `'7'`  ·  `float('3.14')` → 소수 `3.14`""")

code("""# 💻 따라치기 — 글자를 숫자로 바꿔 계산하기
raw_value = '7'          # 파일에서 읽어온 값은 이렇게 '글자'인 경우가 많다
count = int(raw_value)   # int()로 진짜 숫자로 바꾼다

print(count + 1)         # 이제 계산이 된다""")

md("""**✅ 이렇게 나오면 성공**
```
8
```
> 만약 `int()` 없이 `'7' + 1` 을 하면? 글자와 숫자는 못 더해서 **에러**가 나요. (그래서 형변환이 중요!)""")

code("""# ✍️ 직접 바꿔보기 — raw_value 를 다른 글자 숫자로 바꿔 계산해보세요.
#    그리고 (도전!) '7건' 처럼 글자가 섞인 값으로 바꾸면 무슨 일이 나는지도 실험해보세요.

raw_value = '7'          # ← '20' 등으로 바꿔보고, 나중에 '7건' 으로도 실험!
count = int(raw_value)
print(count + 1)""")

md("""### 📝 메모

- `'7건'` 으로 바꿨을 때 나온 에러 메시지(맨 아랫줄)를 적어보세요:""")

md("""### ❓ 확인질문

`int('7건')` 처럼 숫자와 글자가 섞여 있으면 어떤 일이 벌어질까요?

<details><summary>🤔 힌트 보기</summary>

파이썬은 `'7건'` 에서 숫자만 똑똑하게 골라내지 **못해요.** 그래서 `ValueError` 라는 에러가 납니다.
</details>""")

# ══ 마무리 ═════════════════════════════════════════════
md("""---
## 🎯 오늘 요약 (한눈에)

- **변수** = 값에 붙이는 이름표. 자료형은 4가지 — `int`(정수) `float`(실수) `str`(글자) `bool`(참거짓)
- **f-string** `f'... {변수} ...'` 으로 값을 문장에 끼워 예쁘게 출력
- **리스트** `[ ]` = 순서대로 담기(0부터!), **딕셔너리** `{ }` = 이름표로 담고 꺼내기(`.get()` 이 안전)
- **형변환** — 파일에서 온 숫자는 '글자'라 `int()` 로 바꿔야 계산됨 (안 하면 에러!)""")

md("""## 🧩 퀴즈 (스스로 점검)

**Q1.** `type(3.14)` 의 결과는 무엇일까요?
<details><summary>정답 보기</summary>`<class 'float'>` — 소수점이 있으니 실수(float)!</details>

**Q2.** `users = ['kim01', 'lee02', 'park03']` 에서 **두 번째** 값 `'lee02'` 를 꺼내려면?
<details><summary>정답 보기</summary>`users[1]` — 파이썬은 0부터 세니까 두 번째는 1번!</details>

**Q3.** 파일에서 읽은 `'10'` 에 5를 더해 15를 만들려면 어떻게 해야 할까요?
<details><summary>정답 보기</summary>`int('10') + 5` — 먼저 `int()` 로 진짜 숫자로 바꾼 뒤 더한다.</details>""")

md("""## ⏭️ 다음 시간 예고

3교시에는 **조건문(`if`)과 반복문(`for`)** 을 배웁니다.
오늘 만든 로그 딕셔너리들을 **주르륵 훑으면서 "실패한 로그만" 골라내는** 진짜 필터링을 해볼 거예요. 💪

수고했어요! 오늘 배운 걸로 벌써 로그 한 줄을 다룰 수 있게 됐답니다. 🎉""")

# ── 노트북 조립 & 저장 ─────────────────────────────────
nb = {
    "cells": cells,
    "metadata": {
        "kernelspec": {"display_name": "Python 3", "language": "python", "name": "python3"},
        "language_info": {"name": "python", "version": "3.11"}
    },
    "nbformat": 4,
    "nbformat_minor": 5
}

out = "/Users/hong/jm-teaching-material/26-09-ai-security-automation-bootcamp/03-samples/day01-2교시-파이썬기초.ipynb"
os.makedirs(os.path.dirname(out), exist_ok=True)
with open(out, "w", encoding="utf-8") as f:
    json.dump(nb, f, ensure_ascii=False, indent=1)
print("cells:", len(cells))
print("saved:", out)
