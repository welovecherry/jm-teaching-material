# -*- coding: utf-8 -*-
# Day 1 · 리스트와 딕셔너리 v7 — 함수/메서드 선행설명 + 단계지시형 문제 + 접힌 힌트
import json, os
cells=[]
def md(t): cells.append({"cell_type":"markdown","metadata":{},"source":t})
def code(t): cells.append({"cell_type":"code","metadata":{},"execution_count":None,"outputs":[],"source":t})
def blank(): code("# ✍️ 여기에 직접 작성해 보세요\n")
def prob(title, steps, expected, hints):
    # 단계 지시형 문제(마크다운) + 빈 코드 셀 + 접힌 힌트
    md(f"""### ✍️ 문제 — {title}

{steps}

**기대 출력**
```
{expected}
```
<details><summary>힌트 보기 (막힐 때만 펼치세요)</summary>

{hints}
</details>""")
    blank()

# ═══ 표지 ═══
md("""# Day 1 — 리스트와 딕셔너리

## 여러 값을 한 번에 다루기 · 80–105분

> **만든 날짜:** 2026-07-13 · **버전:** v7

변수 하나에 값 하나를 담아 봤습니다. 이제 **여러 값**을 한 번에 담는 두 그릇,
**리스트**와 **딕셔너리**를 배웁니다. 예제를 본 직후 바로 **직접 작성하는 문제**가 이어집니다.""")

# ═══ 학습목표 ═══
md("""## 학습목표

1. 리스트로 여러 값을 담고, 번호(인덱스)로 꺼내기
2. `len()`으로 개수 세기, `append()`로 값 추가하기 (함수·메서드가 무엇인지 함께 이해)
3. 딕셔너리로 이름표-값을 담고, 이름으로 꺼내기
4. `.get()`으로 없는 키도 안전하게 다루기""")

# ═══ 사용법 ═══
md("""## 이 노트북 쓰는 법

- 회색 코드 칸을 클릭하고 **`Shift + Enter`** 로 한 칸씩 실행합니다.
- **예제(💻)를 실행해 확인한 뒤, 바로 아래 문제(✍️)를 직접 작성해 보세요.**
- 문제가 막히면 **"힌트 보기"** 를 펼쳐 참고하세요(정답은 없습니다. 스스로 완성해 보기).

| 표시 | 뜻 |
|------|-----|
| 📖 | 개념(읽기) |
| 💻 | 예제(실행해 결과 확인) |
| ✍️ | 문제(직접 작성) |
| ✅ | 결과("이렇게 나오면 성공") |
| ❓ | 핵심 질문(답은 본문에) |

> 뒤쪽 **`[심화·자습]`** 은 시간이 남거나 자습·리추얼 때 풀면 됩니다.""")

# ══════════════════════════════════════════════════
# 1. 리스트
# ══════════════════════════════════════════════════
md("""---
## 1. 리스트 (list) — 여러 값을 '순서대로'

- 사용자가 여러 명, 로그가 여러 줄일 때 값마다 변수를 만들면 번거롭습니다.
- **리스트**는 여러 값을 **순서대로** 한 그릇에 담습니다. 대괄호 `[ ]` 안에 쉼표로 나열합니다.

> **왜 리스트를 쓸까?** "값이 여러 개"일 때 하나로 묶어 관리하기 위해서입니다.""")

code('''# 💻 예제 — 리스트 만들고 전체 출력하기
users = ["kim01", "lee02", "park03"]   # 값 3개를 순서대로 담음
print(users)''')
md("""**✅ 성공 출력**
```
['kim01', 'lee02', 'park03']
```""")

prob("여러 값을 담는 '리스트' 만들기",
"""1. `servers` 라는 리스트를 만드세요.
2. 그 안에 문자열 `"w1"`, `"w2"` 두 개를 담으세요.
3. `servers` 를 출력하세요.""",
"['w1', 'w2']",
"""- 리스트는 대괄호 `[ ]` 안에 값을 쉼표로 나열합니다.
- 글자(문자열)는 따옴표로 감쌉니다: `"w1"`.
- 출력은 `print(...)`.""")

md("""### 📖 번호(인덱스)로 꺼내기

- 리스트의 값은 **번호(인덱스)** 로 꺼냅니다: `리스트[번호]`
- 번호는 **0부터** 셉니다. 첫 번째 = `[0]`, 두 번째 = `[1]`.""")

code('''# 💻 예제 — 번호로 값 꺼내기
users = ["kim01", "lee02", "park03"]
print(users[0])   # 첫 번째
print(users[1])   # 두 번째''')
md("""**✅ 성공 출력**
```
kim01
lee02
```""")

code('''# ✍️ 문제 (출력 예측) — 무엇이 나올지 먼저 예측한 뒤 실행해 맞춰 보세요.
users = ["kim01", "lee02", "park03"]
print(users[2])   # 예측: ?''')

# --- 함수/메서드 맛보기 (전제 개념) ---
md("""### 📖 잠깐 — '함수'와 '메서드'가 뭔가요?

이제 `len(...)` 과 `append(...)` 가 나옵니다. 처음 보는 모양이죠? 지금은 딱 이만큼만 알면 됩니다.

1. **함수** = 미리 만들어진 명령. **이름 뒤 괄호에 값을 넣어** 어떤 일을 시킵니다.
   예: `len(users)` → 리스트의 개수를 세어 줍니다.
2. **메서드** = **값에 점(`.`)을 찍어 붙는** 함수. 예: `users.append("x")` → 그 리스트에 값을 추가합니다.

> 함수를 **직접 만드는 법**은 Day 2에서 배웁니다. 지금은 "가져다 쓰는 도구"로만 알면 충분합니다.""")

md("""### 📖 개수 세기(len)와 값 추가(append)

- `len(리스트)` → 값의 **개수** (함수)
- `리스트.append(값)` → 맨 **뒤에 값 하나 추가** (메서드)""")

code('''# 💻 예제 — 개수와 추가
users = ["kim01", "lee02"]
print(len(users))       # 2
users.append("park03")  # 뒤에 추가
print(users)
print(len(users))       # 3''')
md("""**✅ 성공 출력**
```
2
['kim01', 'lee02', 'park03']
3
```""")

prob("리스트에 값 추가하고 개수 세기",
"""1. `logs` 라는 리스트를 만들고 문자열 `"e1"`, `"e2"` 를 담으세요.
2. `logs` 에 `"e3"` 을 **추가**하세요.
3. `logs` 의 **개수**를 출력하세요.""",
"3",
"""- 추가는 메서드 `logs.append("e3")`.
- 개수는 함수 `len(logs)`.
- 마지막에 `print(len(logs))`.""")

prob("직접 만들기 — 포트 목록 (백지)",
"""1. `ports` 라는 리스트에 **정수** `22`, `80`, `443` 을 담으세요.
2. **첫 번째 값**과 **개수**를 각각 출력하세요.""",
"22\n3",
"""- 정수는 따옴표 없이: `22`, `80`, `443`.
- 첫 번째 값은 `ports[0]`.
- 개수는 `len(ports)`.""")

# ══════════════════════════════════════════════════
# 2. 딕셔너리
# ══════════════════════════════════════════════════
md("""---
## 2. 딕셔너리 (dict) — '이름표'로 꺼내기

- 리스트는 "몇 번째"로 꺼냅니다. 그런데 로그 한 줄은 *user·event·ip* 처럼 **이름 있는 정보**입니다.
- **딕셔너리**는 **이름표(키)-값** 쌍으로 담고, **이름으로** 꺼냅니다. 중괄호 `{ }` 를 씁니다.

> **왜 딕셔너리를 쓸까?** `log["user"]` 가 `log[0]` 보다 "무엇을 꺼내는지" 훨씬 분명하기 때문입니다.""")

code('''# 💻 예제 — 딕셔너리 만들고 이름으로 꺼내기
log = {"user": "kim01", "event": "login_failed"}
print(log["user"])    # "user" 이름표의 값
print(log["event"])   # "event" 이름표의 값''')
md("""**✅ 성공 출력**
```
kim01
login_failed
```""")

prob("딕셔너리 만들고 값 꺼내기",
"""1. `log` 라는 딕셔너리를 만드세요. 이름표 `"user"` 에 `"lee02"`, `"event"` 에 `"logout"` 을 담으세요.
2. `log` 에서 `event` 값을 꺼내 출력하세요.""",
"logout",
"""- 딕셔너리: `{"user": "lee02", "event": "logout"}`.
- 이름으로 꺼내기: `log["event"]`.""")

md("""### 📖 키가 여러 개일 때

- 딕셔너리에는 이름표-값 쌍을 여러 개 담을 수 있습니다.
- 보안 로그 한 줄을 통째로 딕셔너리로 표현하면 다루기 편합니다.""")

code('''# 💻 예제 — 로그 한 줄을 통째로
log = {
    "customer": "hanbit",
    "user": "kim01",
    "event": "login_failed",
    "ip": "203.0.113.5",
}
print(log["customer"])
print(log["ip"])''')
md("""**✅ 성공 출력**
```
hanbit
203.0.113.5
```""")

code('''# ✍️ 문제 (출력 예측) — 무엇이 나올지 예측한 뒤 실행하세요.
log = {"customer": "hanbit", "user": "kim01", "event": "login_failed"}
print(log["user"])     # 예측: ?
print(log["event"])    # 예측: ?''')

md("""### 📖 없는 키도 안전하게 — `.get()`

- 대괄호 `log["없는키"]` 로 **없는 이름표**를 꺼내면 **에러가 나서 멈춥니다.**
- `log.get("없는키")` 는 없으면 조용히 `None`("값 없음")을 돌려주어 멈추지 않습니다.""")

code('''# 💻 예제 — 대괄호 vs .get()
log = {"user": "kim01"}
print(log.get("user"))     # 있으면 값 → kim01
print(log.get("country"))  # 없으면 None''')
md("""**✅ 성공 출력**
```
kim01
None
```""")

code('''# ✍️ 문제 (버그 수정) — 아래는 없는 키 "ip" 를 대괄호로 꺼내 에러가 납니다.
#   .get() 을 쓰도록 한 줄만 고쳐 None 이 나오게 하세요.  기대 출력: None
log = {"user": "kim01"}
print(log["ip"])   # ← 여기가 문제''')

prob("직접 만들기 — 서버 정보 딕셔너리 (백지)",
"""1. `server` 라는 딕셔너리를 만드세요. `"name"` 에 `"web01"`, `"port"` 에 정수 `8080` 을 담으세요.
2. `name` 값과 `port` 값을 각각 출력하세요.""",
"web01\n8080",
"""- 값의 종류가 섞여도 됩니다: 글자 `"web01"`, 정수 `8080`.
- 꺼내기: `server["name"]`, `server["port"]`.""")

md("""### ❓ 핵심 질문

`log["event"]` 대신 `log.get("event")` 를 쓰면 **어떤 상황에서 더 안전**할까요?
> 답은 위 `.get()` 설명 안에 있습니다. "없는 키를 꺼낼 때"를 떠올려 보세요.""")

# ══════════════════════════════════════════════════
# [심화·자습]
# ══════════════════════════════════════════════════
md("""---
## [심화·자습] — 더 배우기
> 시간이 남거나 자습·리추얼 때 풀어 보세요.

### 📖 딕셔너리 훑어보기 — keys / values / items
- `keys()` 이름표만 / `values()` 값만 / `items()` 이름표+값 쌍 (모두 메서드)""")

code('''# 💻 (심화) keys / values
log = {"user": "kim01", "event": "login_failed"}
print(list(log.keys()))     # 이름표들
print(list(log.values()))   # 값들''')
md("""**✅ 성공 출력**
```
['user', 'event']
['kim01', 'login_failed']
```""")

code('''# ✍️ (심화·출력 예측) items() 로 반복하면 무엇이 나올지 예측한 뒤 실행하세요.
log = {"user": "kim01", "event": "login_failed"}
for key, value in log.items():
    print(key, value)     # 예측: ?''')

md("""### 📖 리스트 + 딕셔너리 조합 (실무에서 가장 많이 씀)
- 로그 **여러 줄** = "딕셔너리(로그 한 줄)를 리스트에 담기".
- 이 구조가 앞으로 로그를 훑고 거르는 실습의 핵심 재료가 됩니다.""")

code('''# 💻 (심화) 딕셔너리들의 리스트
logs = [
    {"user": "kim01", "event": "login_failed"},
    {"user": "lee02", "event": "login_success"},
]
print(logs[0]["user"])    # 첫 번째 로그의 user
print(logs[1]["event"])   # 두 번째 로그의 event''')
md("""**✅ 성공 출력**
```
kim01
login_success
```""")

prob("직접 만들기 — 조합 구조에서 꺼내기 (백지)",
"""아래 `logs` 가 주어졌다고 할 때, **첫 번째 로그의 `event` 값**을 꺼내 출력하세요.
```
logs = [
    {"user": "kim01", "event": "login_failed"},
    {"user": "lee02", "event": "login_success"},
]
```""",
"login_failed",
"""- 먼저 첫 번째 로그를 고르고: `logs[0]`.
- 그 안에서 이름으로 꺼내기: `logs[0]["event"]`.""")

md("""> **집합(set)·튜플(tuple)** 은 뒤 차시에서 배웁니다(집합은 오늘 강의2의 '중복 제거·집계'에서).

### ❓ 면접 질문
**"리스트와 딕셔너리의 차이는 무엇이며, 보안 로그 한 줄엔 어느 것이 더 적합한가요?"**
> 힌트: "번호로 꺼낸다 vs 이름으로 꺼낸다". 위 개념 설명에서 답을 찾아보세요.""")

# ═══ 마무리 ═══
md("""---
## 오늘 요약

1. **리스트 `[ ]`** — 여러 값을 순서대로. 번호(0부터)로 꺼내고, `len()` 개수, `append()` 추가.
2. **함수/메서드** — `len(값)` 은 함수, `값.append(...)` 은 메서드(값에 붙는 함수).
3. **딕셔너리 `{ }`** — 이름표(키)-값. 이름으로 꺼냄. 보안 로그 한 줄에 적합.
4. **`.get()`** — 없는 키도 에러 없이 `None`. (심화) **딕셔너리들의 리스트** = 로그 여러 줄.""")

md("""## 종합 퀴즈 (객관식)

**Q1.** `users = ["kim01", "lee02", "park03"]` 에서 두 번째 값을 꺼내려면? ① `users[1]` ② `users[2]` ③ `users["1"]`
<details><summary>정답 보기</summary>① `users[1]` — 0부터 세므로 두 번째는 1번</details>

**Q2.** 리스트 맨 뒤에 값을 추가하는 메서드는? ① `add()` ② `append()` ③ `push()`
<details><summary>정답 보기</summary>② `append()`</details>

**Q3.** 딕셔너리에서 없을 수도 있는 키를 **에러 없이** 꺼내려면? ① `log["key"]` ② `log.get("key")` ③ `log(key)`
<details><summary>정답 보기</summary>② `log.get("key")` — 없으면 None 을 돌려줌</details>""")

md("""## 다음 시간 예고

이어서 **연산자와 형변환**을 배우고, 3교시(강의2)에서 **조건문·반복문**으로
오늘 만든 로그(딕셔너리들의 리스트)를 훑어 "실패한 로그만" 골라내는 실제 필터링을 합니다.""")

# ═══ 저장 ═══
nb={"cells":cells,"metadata":{"kernelspec":{"display_name":"Python 3","language":"python","name":"python3"},"language_info":{"name":"python","version":"3.11"}},"nbformat":4,"nbformat_minor":5}
out="/Users/hong/jm-teaching-material/26-09-ai-security-automation-bootcamp/03-samples/day01-리스트와-딕셔너리-v7.ipynb"
os.makedirs(os.path.dirname(out),exist_ok=True)
with open(out,"w",encoding="utf-8") as f: json.dump(nb,f,ensure_ascii=False,indent=1)
print("cells:",len(cells)); print("saved:",out)
